# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Runtime data (SQLite lives here, see config db.path)
data/

# Environment — real secrets never enter the repo; .env.example is the template
.env
.env.*
!.env.example

# Test / coverage output
coverage/

# OS / editor noise
.DS_Store

# Internal planning document — deliberately untracked (history-purged before the first public push)
PLAN.md
