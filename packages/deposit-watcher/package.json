{
  "name": "@chainvue/v402-deposit-watcher",
  "version": "0.1.0",
  "description": "Deposit watcher for the v402 facilitator: real mode (block polling, reorg detection, sender-VerusID attribution, confirmation crediting, re-mine handling) and simulated mode for dev/CI without a Verus node.",
  "license": "Apache-2.0",
  "author": "Robert Lech (chainvue)",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@chainvue/v402-protocol": "workspace:*",
    "@chainvue/v402-storage": "workspace:*",
    "@chainvue/v402-verus-rpc": "workspace:*"
  }
}
