{
  "name": "@chainvue/v402-verus-rpc",
  "version": "0.1.0",
  "description": "Typed Verus daemon JSON-RPC client for v402: the RPC methods the payment stack needs, wrapped in a 500ms-timeout + circuit-breaker policy. Includes a mock implementation for tests.",
  "license": "Apache-2.0",
  "author": "Robert Lech (chainvue)",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "cockatiel": "^4.0.0"
  }
}
